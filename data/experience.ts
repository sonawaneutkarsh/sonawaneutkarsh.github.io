import type { ExperienceEntry } from "./types";

// TODO: No formal work experience yet. A DataPhi Dubai internship is in the
// works but not confirmed to start — add an entry here once it does.
export const experience: ExperienceEntry[] = [];
